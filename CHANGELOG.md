# Changelog

## Unreleased

- added a new `student_consultancy_*` Community-first addon suite under `addons/product/`
- added `student_consultancy_180dc_escp` as a thin distribution addon
- introduced docs for architecture, install, publishing, dependencies, and upgrades
- switched repository validation to recursive addon discovery and product packaging checks
- removed legacy `x_180dc_*` and Authentik instance modules
- removed backward-compat root scripts and templates in favor of `ops/`
